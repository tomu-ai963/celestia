# CELESTIA — 出生天球

出生の日時と場所から、その瞬間の空を3Dで再構成する天文ビューア。
占い的な解釈は持たず、**天体が実際にどこにいたか**だけを描く。

依存は three.js (r128, CDN) のみ。ES モジュールと `data/*.json` の fetch を使うため、
表示にはローカルHTTPサーバーが必要（下記「ローカル確認」参照）。

---

## 実装済み（プロトタイプ v0.1）

| 領域 | 内容 |
|---|---|
| 暦法 | グレゴリオ暦 → ユリウス日（`julianDay`）。タイムゾーンは出生地プリセットから固定オフセット |
| 惑星 | JPL近似軌道要素（1800–2050）＋ケプラー方程式のニュートン反復。ヘリオ座標から地球位置を引いて地心化 |
| 月 | 短縮ELP級数（黄経19項 / 黄緯9項 / 距離5項）。精度は黄経で概ね ±0.01° |
| 太陽 | 地球ヘリオベクトルの反転 |
| 感受点 | 月の平均交点、平均リリス（軌道遠地点）を平均要素から算出 |
| 座標変換 | 黄道直交座標 → three.js 座標は `(x, z, -y)` で統一 |
| 地平線 | GMST → 地方恒星時 → 天頂ベクトルを黄道座標に変換し、直交する大円を描画 |
| 星野 | HYG v4.1 実データ 8,920星（等級6.5以下）。ビルド時に黄道座標へ変換・JSON化（178KB / gzip 78KB） |
| 星座線 | Stellarium western skyculture の HIP ポリラインを HYG と照合して座標解決。黄道12星座はゴールドで強調、他76星座は暗く描画 |
| 歳差 | 星野・星座線・IAU境界・天の川を親グループにまとめ、黄道極まわりに `1.39697°×T` 回転（惑星計算は当日座標のまま） |
| 天の川 | 銀河座標系の向きだけ正確な手続き生成の装飾レイヤー（銀緯ガウス減衰・銀河中心側が明るい非対称）。**天文データではない** |
| 時間軸 | 時/日/月/年のスケール切替、再生・速度（±1〜64倍）・スクラブ・出生時刻へのリセット。範囲は 1800–2050 |
| 逆行 | 黄経の中心差分（0°/360°のラップ考慮）で判定。読み出しとカードに ℞ 表示、軌跡の逆行区間をオレンジで色分け |
| トランジット | 出生図の天体位置を薄いマーカーで残し、現在位置との黄経差をカードに表示（解釈は持たない） |
| 星座 | 均等30度のサインと、IAU境界（蛇遣座を含む）を歳差補正して重ね、ズレを可視化 |

### 設計上の前提 — 時間軸まわりの判断

**地平線は時/日スケールでは時刻に追従し、月/年スケールでは出生時で固定する。**
地平線は地方恒星時に依存するため1日で天球を一周する。月・年スケールでは1フレームで
何十回転もしてしまい、可視化として意味をなさない（点滅にしか見えない）ため固定した。
固定中はトグルのラベルが「地平線（出生時）」に変わる。

**再生・スクラブ中は位置の補間（lerp）を挟まない。** 補間は「天球を生成」や
トグル操作のときに位置がなめらかに移り変わるための演出で、時間が動いているときは
動き自体がアニメーションなので、補間があるとスクラブが遅れて追従する。
時間が動いているときだけ即時反映に切り替えている。

**歳差は時刻に追従する。** 天球固定レイヤー（星野・星座線・IAU境界・天の川）の
親グループを毎回 `1.39697°×T` 回転させ直すので、年スケールで動かすと星野が惑星と
同じ座標系のまま追従する（1800年↔2050年で星野が約3.5°動く）。

### 設計上の前提 — サインと実星座

ホロスコープの「サイン」は黄道の均等30度分割であり、実在の星座の大きさとも位置とも一致しない。
歳差により J2000 から現在まで約24度ずれている。本アプリはこのズレを隠さず、
`実際の星座` トグルで両者を重ねて見せることを演出の中心に据えている。

---

## 次工程のロードマップ

### P1 — 星野の実データ化
- [x] HYG database (v4.1) を取り込み、等級6.5までの8,920星に差し替え
- [x] 赤道座標 → 黄道座標の変換、等級 → 点サイズ / 色指数 → 色 のマッピング
- [x] 星座線（Stellarium western skyculture）を描画、黄道12星座をゴールドで強調・他は暗く
- [x] データはビルド時に間引いてJSON化（初期ロード 200KB 以下を目標 → 実測 178KB / gzip 78KB）

### P1.5 — P1 レビュー対応
- [x] 座標系の統一: 天球固定レイヤーを歳差回転させ、当日座標の惑星と整合
      （回転の符号は 3D 座標からの逆算テストで確定）
- [x] 天の川を装飾レイヤーとして復活（銀河座標系から向きを算出）
- [x] `data/ATTRIBUTION.md` で CC BY-SA の帰属・変更内容を明示
- [x] 歳差と座標規約を `src/precession.mjs` に一本化し、本体とテストが同じ実装を参照
      （テスト側の参照実装は削除。本体の符号を変えるとテストが落ちる）

### P2 — 時間軸
- [x] タイムスライダーで日時を連続変更、天体位置を再計算して表示（スケール切替つき）
- [x] 逆行の可視化（黄経の微分が負の区間を軌跡上でマーク）
- [x] 出生時刻から任意の日時までのトランジット再生（出生位置マーカーとの対比）

### P3 — 精度と検証
- [ ] 惑星位置を JPL Horizons の値と突き合わせるテストを追加（許容 ±1 arcmin）
- [ ] 月の黄経を Meeus 完全版と比較
- [ ] タイムゾーンを固定オフセットから IANA tz データベースへ（夏時間対応）
- [ ] 出生地の入力をプリセットから住所ジオコーディングへ

### P4 — 占星術レイヤー（分岐）
- [ ] ハウス分割（プラシーダス / イコール）、ASC / MC の算出と描画
- [ ] アスペクト線（メジャー5種、オーブ設定可）
- [ ] とむMYSTIC の解釈テキストをフォーカスカードに接続

### 運用
- [ ] GitHub Pages で公開
- [ ] OGP画像を天球のスクリーンショットから自動生成
- [ ] three.js を CDN からローカルバンドルへ（オフライン動作）

---

## 開発

星野データの再生成とテスト（Node 18+ が必要。生成済み `data/stars.json` はコミットされているので通常は不要）:

```sh
npm run build   # HYG v4.1 と Stellarium western skyculture をダウンロードして
                # data/stars.json と data/constellations.json を生成（tools/.cache にキャッシュ）
npm test        # 19件: 座標変換（4恒星 ±0.1°）/ 歳差回転の符号 / 逆行判定 / データ形式
```

天文計算と歳差は `src/astro.mjs` と `src/precession.mjs` にあり、`index.html`（ESモジュール）と
テストが同じ実装を参照する。参照実装をテスト側に複製していないので、本体の符号や
座標規約を変えるとテストが落ちる。

### ローカル確認

ES モジュールと `data/*.json` の fetch を使うため **file:// で index.html を直接開くと動作しない**
（その旨の案内だけが表示される）。リポジトリ直下でHTTPサーバーを立てて開く:

```sh
npm run serve                 # npx serve（http://localhost:8000）
# または
python -m http.server 8000    # http://localhost:8000
```

## 既知の制約

- 惑星の精度は 1800–2050 の範囲外で急速に劣化する（JPL近似要素の適用範囲）
- タイムゾーンは固定オフセットのため、夏時間期間の出生時刻は最大1時間ずれる
- IAU 星座境界は黄道上の交点のみを近似した13区分で、実際の多角形境界ではない
- ES モジュールと fetch を使うため file:// では動作しない（案内だけ出して起動しない）
- 時間軸の範囲は 1800–2050（JPL近似要素の適用範囲）。端に達すると再生は自動で止まり、
  日時表示に「下限 1800 / 上限 2050」が出る
- 逆行判定は黄経の中心差分（±0.5日）。留の前後 ±0.5日ほどは判定が切り替わる途中になる
- 軌跡はスケールごとの保持期間でサンプリングするため、サンプル間隔より速く動く天体
  （年スケールの月・水星・金星など）は折り返しを表現できないので描画しない
- 出生位置マーカーはその日の黄道座標（トロピカル）のまま置いており、歳差ぶんの
  座標系の違いは補正していない（サイン上の位置を比べる用途に合わせたため）
- 天の川は**装飾レイヤー**であり天文データではない。銀河座標系の向き（銀河北極・銀河中心の
  J2000 値）だけを正確に再現した手続き生成の点群で、HYG 実星野とは別物
- 星の色は B-V → 色温度（Ballesteros近似）→ 黒体色 の近似変換。ci 欠損の40星は
  カタログ平均値 0.63 で代替
- 歳差は天球固定レイヤー（星野・星座線・IAU境界・天の川）全体を黄道極まわりに
  `1.39697° × T` 回転させる近似で適用。恒星の固有運動・章動・極運動は反映しない
- 星座線は Stellarium western skyculture 由来の6点の HIP が HYG に見つからず、該当箇所で
  ポリラインを分割している（見た目にはほぼ影響しない）
- `data/` 配下は CC BY-SA の派生データでコード（MIT）と別ライセンス。
  帰属・変更内容は [data/ATTRIBUTION.md](data/ATTRIBUTION.md) を参照

## ライセンス

- **コード**（index.html / src/ / tools/ / tests/）: MIT（[LICENSE](LICENSE)）
- **データ**（data/ 配下）: CC BY-SA — HYG Database v4.1 と Stellarium sky cultures の派生物。
  帰属表示・ライセンスリンク・変更内容の明示は [data/ATTRIBUTION.md](data/ATTRIBUTION.md)
